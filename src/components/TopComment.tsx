import React from "react";
import "./component_styles/TopComment.css";

const TopComment = () => {
  return (
    <div className="top_comment_container">
      <h3 className="header">Some readers wanted to add context</h3>
      <p className="content">
        Although this and that, therefore there could be no that and this bruh
        huh?
      </p>
    </div>
  );
};

export default TopComment;
